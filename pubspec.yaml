name: rick_and_morty_app
description: "Rick and Morty application with using API"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  connectivity_plus: ^5.0.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  flutter_bloc: ^8.1.5
  http: ^1.2.1
  shared_preferences: ^2.2.2
  internet_connection_checker: ^1.0.0+1
  get_it: ^7.6.7
  cached_network_image: ^3.3.1
  bloc: ^8.1.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0


flutter:

  uses-material-design: true

assets:
  - assets/images/image_placeholder.jpeg
